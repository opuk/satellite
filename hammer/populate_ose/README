Run in this order:

* populate-docker.sh
* enable_rpms.sh
* populate_cv.sh

base-images.sh will only enable the base images needed for Openshift and none of the builder images, populate-docker.sh could be used instead
